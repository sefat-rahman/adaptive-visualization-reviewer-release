# Adaptive OpenLayers Data

This directory holds the local working data for the OpenLayers application.

## Structure

- `generated_data/`
  Precomputed monthly datasets used by the app.
- `cache/`
  Downloaded or generated boundary caches and other saved runtime artifacts.

The app defaults to `generated_data/2016-12`.
